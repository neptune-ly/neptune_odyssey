{
  "name": "neptune-odyssey",
  "version": "2.0.0",
  "private": true,
  "description": "Neptune Odyssey — a vendor-neutral, white-label banking design system by Neptune.Fintech. One Material 3 + M3 Expressive core; any institution wears it as a theme, never a fork.",
  "license": "SEE LICENSE IN LICENSE",
  "author": "Neptune.Fintech (https://neptune.ly)",
  "homepage": "https://neptune.ly",
  "repository": {
    "type": "git",
    "url": "https://github.com/neptune-ly/neptune_odyssey.git"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" run build",
    "test": "pnpm -r --filter \"./packages/**\" run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "tokens:build": "pnpm --filter @neptune.fintech/tokens run build",
    "ci": "pnpm run build && pnpm run test",
    "site:bundle": "node site/bundle/build.mjs",
    "codegen": "node tools/codegen.mjs",
    "codegen:check": "node tools/codegen.mjs --check",
    "contrast": "node tools/contrast-check.mjs",
    "client-demo": "node tools/client-demo/generate.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
